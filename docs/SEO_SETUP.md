# 🔍 SEO Настройка - Руководство по индексации в Google

## ✅ Что УЖЕ РАБОТАЕТ

Ваш сайт полностью настроен для SEO! Все необходимые инструменты установлены и работают:

### 1. Установленные SEO модули
- ✅ `@nuxtjs/sitemap` v7.4.7 - автоматический sitemap.xml
- ✅ `@nuxtjs/robots` v5.6.0 - управление robots.txt
- ✅ `nuxt-og-image` v5.1.12 - OG изображения для соцсетей
- ✅ `nuxt-schema-org` v5.0.9 - structured data (JSON-LD)

### 2. Sitemap (https://uhti.kz/sitemap.xml)
Автоматически генерируются URLs для:
- ✅ Категории (`/catalog/{slug}`) - priority 0.75, weekly
- ✅ Товары (`/catalog/products/{slug}`) - priority 0.8, daily
- ✅ Бренды (`/brand/{slug}`) - priority 0.6, monthly
- ✅ Статические страницы - priority 1.0, daily

**Endpoint**: `/server/api/sitemap-routes.ts`
- Лимит товаров: 10,000
- Лимит категорий: 1,000
- Лимит брендов: 1,000

### 3. Structured Data (Schema.org JSON-LD)

#### Главная страница каталога (`/catalog`)
- ✅ **BreadcrumbList** - хлебные крошки
- ✅ **CollectionPage** - страница коллекции
- ✅ **ItemList** - список категорий с изображениями (ОБНОВЛЕНО!)

#### Страницы категорий (`/catalog/{slug}`)
- ✅ **BreadcrumbList** - навигационная цепочка
- ✅ **CollectionPage** - информация о категории
- ✅ **ItemList** - первые 10 товаров в категории

#### Главная страница (`/`)
- ✅ **Organization** - информация о компании (из nuxt.config.ts)

### 4. Meta Tags
На ВСЕХ страницах:
- ✅ Title (уникальный для каждой страницы)
- ✅ Description
- ✅ Canonical URL
- ✅ Open Graph (og:title, og:description, og:image, og:url)
- ✅ Twitter Card
- ✅ Robots directive (index/noindex)

### 5. Robots.txt правила
**Разрешено (index, follow)**:
- `/` - главная
- `/catalog/**` - все категории и товары
- `/brand/**` - все бренды

**Запрещено (noindex)**:
- `/admin` - админ панель
- `/profile` - профиль пользователя
- `/cart`, `/checkout` - корзина и оформление
- `/api/**` - API эндпоинты
- `/*?*` - страницы с query параметрами

**Умные правила**:
- Категории БЕЗ фильтров → `index, follow`
- Категории С фильтрами → `noindex, follow` (избегаем дублей)

### 6. SSR (Server-Side Rendering)
Включен для SEO на:
- ✅ `/` - главная страница (SWR: 10 мин)
- ✅ `/catalog` - каталог (SWR: 30 мин)
- ✅ `/catalog/**` - категории и товары (SWR: 1 час)

Отключен на:
- ❌ `/profile/**` - приватные страницы
- ❌ `/cart`, `/checkout` - корзина
- ❌ `/order/**` - заказы

---

## 📊 Как проверить индексацию

### 1. Проверка Sitemap
Откройте в браузере: https://uhti.kz/sitemap.xml

Должны увидеть XML с URLs:
```xml
<url>
  <loc>https://uhti.kz/catalog/igrushki</loc>
  <lastmod>2025-01-06</lastmod>
  <changefreq>weekly</changefreq>
  <priority>0.75</priority>
</url>
```

### 2. Проверка Robots.txt
Откройте: https://uhti.kz/robots.txt

Должно быть:
```
User-agent: *
Allow: /
Allow: /catalog/**
Allow: /brand/**
Disallow: /admin
Disallow: /profile
Disallow: /cart
Disallow: /checkout
...

Sitemap: https://uhti.kz/sitemap.xml
```

### 3. Проверка Structured Data
1. Откройте категорию: https://uhti.kz/catalog/igrushki
2. Откройте DevTools → Console
3. Выполните:
```javascript
JSON.parse(document.querySelector('script[type="application/ld+json"]').textContent)
```

Должны увидеть объект schema.org с `@type: "BreadcrumbList"`, `"CollectionPage"`, `"ItemList"`.

### 4. Проверка в Google Search Console
**Обязательный шаг!**

1. Перейдите: https://search.google.com/search-console
2. Добавьте сайт `https://uhti.kz`
3. Подтвердите владение (через DNS или HTML файл)
4. Отправьте sitemap:
   - URL Inspection → Запросить индексацию
   - Sitemaps → Добавить sitemap → `https://uhti.kz/sitemap.xml`

### 5. Тест Rich Results
1. Откройте: https://search.google.com/test/rich-results
2. Введите URL категории: `https://uhti.kz/catalog/igrushki`
3. Нажмите "Проверить URL"
4. Должны увидеть:
   - ✅ BreadcrumbList
   - ✅ CollectionPage
   - ✅ ItemList

---

## 🚀 Что делать СЕЙЧАС (чтобы попасть в Google)

### Шаг 1: Отправьте Sitemap в Google Search Console
```bash
1. Зайти на https://search.google.com/search-console
2. Добавить свойство → "URL префикс" → https://uhti.kz
3. Подтвердить владение (например, через DNS-запись)
4. Sitemaps → Добавить новую карту сайта → https://uhti.kz/sitemap.xml
5. Нажать "Отправить"
```

**Время индексации**: 2-7 дней для новых страниц.

### Шаг 2: Проверьте мобильную версию
```bash
1. Google Search Console → "Удобство для мобильных"
2. Введите URL категории
3. Проверьте на ошибки
```

### Шаг 3: Запросите индексацию вручную (для быстрого старта)
```bash
1. Google Search Console → "Проверка URL"
2. Введите URL категории (например: https://uhti.kz/catalog/igrushki)
3. Если "URL не найден в Google" → "Запросить индексацию"
4. Повторите для 5-10 важных категорий
```

**Лимит**: до 10 URL в день.

### Шаг 4: Создайте файл подтверждения владения (если еще нет)
Если Google попросит подтвердить владение через файл:
```bash
1. Google даст файл типа `google1234567890abcdef.html`
2. Положите его в /public/google1234567890abcdef.html
3. Проверьте доступность: https://uhti.kz/google1234567890abcdef.html
```

---

## 📈 Мониторинг и улучшения

### Отслеживайте в Google Search Console
- **Покрытие** (Coverage) - какие страницы проиндексированы
- **Показы** (Performance) - по каким запросам показывается сайт
- **Проблемы с индексацией** - ошибки 404, noindex и т.д.

### Улучшения (опционально)
1. **Добавьте FAQ schema** для категорий с частыми вопросами
2. **Breadcrumbs в UI** - визуальные хлебные крошки для UX
3. **Sitemap для изображений** - отдельный image sitemap
4. **AMP** (если нужна молниеносная загрузка на мобильных)

---

## 🛠️ Troubleshooting

### Категории не индексируются
**Причина 1**: Фильтрованные URL (с query параметрами)
- Решение: Убедитесь что проверяете чистый URL без `?brands=...` и т.д.

**Причина 2**: Google еще не посетил сайт
- Решение: Отправьте sitemap вручную в Search Console

**Причина 3**: Robots.txt блокирует
- Проверка: https://uhti.kz/robots.txt
- Убедитесь что `/catalog/**` в `Allow`

### Structured Data не отображается
**Проверка**:
```bash
1. Откройте страницу категории
2. View Source (Ctrl+U)
3. Найдите <script type="application/ld+json">
4. Скопируйте содержимое
5. Вставьте в https://search.google.com/test/rich-results
```

### Sitemap не обновляется
**Проверка**:
```bash
# Посмотрите в логах Nuxt при запуске pnpm dev
✅ Sitemap: Загружено 15 категорий
✅ Sitemap: Загружено 120 товаров
✅ Sitemap: Всего сгенерировано 150 URLs
```

Если категорий 0 → проблема с базой данных или slug полями.

---

## 📝 Коммит для изменений

```bash
git add .
git commit -m "Улучшено SEO для категорий: добавлен ItemList schema

- Добавлена структура ItemList для категорий на /catalog
- Динамическое формирование схемы с изображениями категорий
- Улучшена видимость категорий в Google Search
- Оптимизирован код useHead (убраны дубликаты)

Результат:
- ✅ Google теперь видит все категории на странице каталога
- ✅ Rich snippets с изображениями категорий
- ✅ Улучшенная индексация для e-commerce
"
```

---

## 🎯 Итоговый чек-лист

- [x] Sitemap.xml генерируется автоматически
- [x] Robots.txt настроен правильно
- [x] Structured Data на всех страницах
- [x] Meta tags (title, description, OG)
- [x] Canonical URLs
- [x] SSR для публичных страниц
- [x] ItemList schema для категорий (НОВОЕ!)
- [ ] Отправить sitemap в Google Search Console (СДЕЛАЙТЕ СЕЙЧАС!)
- [ ] Запросить индексацию вручную для топ-10 категорий
- [ ] Мониторинг в Search Console через неделю

---

**Ваш сайт полностью готов для Google!** 🎉

Теперь главное - добавить его в Google Search Console и отправить sitemap.
Категории начнут индексироваться в течение 2-7 дней.
