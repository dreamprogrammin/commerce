# 🌐 Мульти-поисковая индексация (IndexNow + Legacy Ping)

## ✅ Что теперь работает

При создании/обновлении товара автоматически отправляются уведомления в **3 системы**:

### 1️⃣ IndexNow (Основной метод) - ✅ Официально поддерживается

**Поисковики:**
- Yandex (Россия, Казахстан)
- Bing (Microsoft)
- Seznam (Чехия)
- Naver (Корея)

**Статус:** ✅ Работает отлично
**Лимит:** БЕЗ лимитов (до 10,000 URL за раз)
**Скорость:** 5-30 минут

### 2️⃣ Yandex Webmaster Ping - ⚠️ Неофициальный

**Endpoint:** `https://webmaster.yandex.com/ping?sitemap=...`

**Статус:** ⚠️ Не документирован, но может работать
**Причина:** Яндекс убрал упоминание из документации в 2017, но endpoint еще отвечает

**Используем как дополнительную страховку** на случай если IndexNow не сработает.

### 3️⃣ Bing Webmaster Ping - ⚠️ Ограниченный

**Endpoint:** `https://www.bing.com/webmaster/ping.aspx?siteMap=...`

**Статус:** ⚠️ Работает только для новых sitemap
**Ограничение:** Не для обновлений существующих sitemap
**Причина:** Bing отключил anonymous ping в мае 2022

**Используем как дополнительную страховку**, хотя эффективность низкая.

---

## 📊 Сравнение методов

| Метод | Статус | Лимит | Скорость | Поисковики |
|-------|--------|-------|----------|------------|
| **IndexNow** | ✅ Официальный | БЕЗ лимитов | ⚡ 5-30 мин | Yandex, Bing, Seznam, Naver |
| **Yandex Ping** | ⚠️ Неофициальный | Неизвестно | 🤷 Может работать | Yandex |
| **Bing Ping** | ⚠️ Устаревший | Неизвестно | 🐌 Только новые sitemap | Bing |
| **Google Ping** | ❌ Отключен | - | - | - |

---

## 🚀 Как это работает

### При создании товара

```typescript
// stores/adminStore/adminProductsStore.ts
async function createProduct(data) {
  const newProduct = await supabase.from('products').insert(data)

  // Автоматически вызывается
  notifySearchEngines(newProduct.slug)
}
```

### Что происходит под капотом

```typescript
// composables/useSeoIndexing.ts
const result = await $fetch('/api/seo/notify-indexing', {
  method: 'POST',
  body: { urls: ['/catalog/products/toy-car'] }
})

// Результат:
{
  success: true,
  indexnow: { submitted: true, urls_count: 1 },
  yandex_ping: { submitted: true },
  bing_ping: { submitted: true }
}
```

### Логи в консоли

```bash
✅ SEO: IndexNow (1 URL) + Yandex Ping + Bing Ping
```

---

## 📈 Реальная эффективность

### IndexNow (100% работает)

**Yandex:**
- ✅ Официально поддерживает IndexNow с 2021 года
- ⚡ Индексация через 5-30 минут
- 🎯 Основной поисковик в Казахстане

**Bing:**
- ✅ Один из создателей IndexNow (вместе с Яндексом)
- ⚡ Индексация через 30-60 минут
- 📈 Растущая популярность

**Seznam, Naver:**
- ✅ Официально поддерживают IndexNow
- 🌍 Региональные лидеры (Чехия, Корея)

### Yandex Ping (неизвестная эффективность)

**Может работать:**
- ✅ Endpoint отвечает (не возвращает 404)
- ⚠️ Не документирован с 2017 года
- 🤷 Неизвестно обрабатывается ли запрос

**Зачем используем:**
- Дополнительная страховка
- Не мешает основным методам
- Нет негативных последствий

### Bing Ping (низкая эффективность)

**Ограничения:**
- ⚠️ Работает только для **новых** sitemap
- ❌ НЕ работает для обновлений
- 📉 Bing рекомендует IndexNow вместо ping

**Зачем используем:**
- Дополнительная страховка при первой отправке sitemap
- Может помочь при первом запуске сайта

---

## 🔍 Проверка индексации

### Яндекс

```bash
# Поиск по сайту
site:uhti.kz название товара

# Яндекс.Вебмастер
https://webmaster.yandex.ru/
```

**Ожидаемое время:** 5-30 минут после создания товара

### Bing

```bash
# Поиск по сайту
site:uhti.kz название товара

# Bing Webmaster Tools
https://www.bing.com/webmasters
```

**Ожидаемое время:** 30-60 минут после создания товара

### Google

```bash
# Поиск по сайту
site:uhti.kz название товара

# Google Search Console
https://search.google.com/search-console
```

**Ожидаемое время:** 3-7 дней (через sitemap краулинг)

---

## ⚙️ Технические детали

### API Endpoint

**Файл:** `/server/api/seo/notify-indexing.post.ts`

**Параметры:**
```typescript
{
  urls: string[]        // Массив URL для индексации
  type?: 'created' | 'updated' | 'deleted'
}
```

**Ответ:**
```typescript
{
  success: boolean
  indexnow: { submitted: boolean, error?: string, urls_count: number }
  yandex_ping: { submitted: boolean, error?: string }
  bing_ping: { submitted: boolean, error?: string }
}
```

### Composable

**Файл:** `/composables/useSeoIndexing.ts`

**Методы:**
```typescript
const { notifyProduct, notifyProducts, notifyBrand, notifyCategory } = useSeoIndexing()

// Уведомить о товаре
await notifyProduct('toy-car-slug')

// Уведомить о нескольких товарах
await notifyProducts(['toy-1', 'toy-2', 'toy-3'])

// Уведомить о бренде
await notifyBrand('lego')

// Уведомить о категории
await notifyCategory('constructor')
```

---

## 🐛 Troubleshooting

### IndexNow не работает

**Проверьте:**
1. `INDEXNOW_KEY` настроен в `.env`
2. Файл `{key}.txt` существует в `/public`
3. Сайт доступен по HTTPS

**Решение:**
```bash
# Проверить ключ
echo $INDEXNOW_KEY

# Проверить файл
curl https://uhti.kz/07d3f5086f59e65326ce9d66b1d1f57c.txt

# Должен вернуть ваш ключ
```

### Yandex Ping возвращает ошибку

**Это нормально!** Yandex Ping — неофициальный метод.

**Действия:**
- ✅ IndexNow работает → всё ОК
- ⚠️ Yandex Ping не работает → не критично

### Bing Ping возвращает ошибку

**Это нормально!** Bing Ping работает только для новых sitemap.

**Действия:**
- ✅ IndexNow работает → всё ОК
- ⚠️ Bing Ping не работает → не критично

### Товары не индексируются в Google

**Это нормально!** Google не поддерживает ping.

**Решение:**
1. Подождать 3-7 дней (sitemap краулинг)
2. Вручную через Google Search Console (10 URL/день)
3. Добавить товар на главную страницу

---

## 📚 Источники

- [IndexNow Protocol](https://www.indexnow.org/)
- [Yandex Webmaster API](https://yandex.com/dev/webmaster/)
- [Bing Webmaster Tools](https://www.bing.com/webmasters)
- [Google прекратил Sitemap Ping (2023)](https://developers.google.com/search/blog/2023/06/sitemaps-lastmod-ping)
- [Bing удалил anonymous sitemap ping (2022)](https://blogs.bing.com/webmaster/may-2022/Spring-cleaning-Removed-Bing-anonymous-sitemap-submission)
- [Bing stops accepting Sitemap pings](https://www.ctrl.blog/entry/sitemap-ping-indexnow.html)

---

## ✅ Итог

**Работающая стратегия:**
1. ✅ **IndexNow** — основной метод (Yandex, Bing, Seznam, Naver)
2. ⚠️ **Yandex Ping** — дополнительная страховка
3. ⚠️ **Bing Ping** — дополнительная страховка
4. 📄 **Google** — естественный краулинг через sitemap.xml

**Для Казахстана:**
- 🎯 Яндекс — основной (IndexNow работает отлично!)
- 📈 Google — вторичный (ждём 3-7 дней)
- 🌍 Bing — дополнительный

**Результат:** Максимальное покрытие поисковых систем с минимальными задержками!

---

**Дата:** 2026-01-25
**Статус:** ✅ Работает (IndexNow + Legacy Ping)
