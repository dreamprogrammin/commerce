# 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: Товары и категории не индексируются Google

## 📊 Диагностика проблемы

### ✅ Что работает правильно:
1. ✅ **Sitemap генерируется** - https://uhti.kz/sitemap.xml доступен
2. ✅ **Robots.txt настроен** - `/catalog/**` разрешен для индексации
3. ✅ **Meta tags присутствуют** - title, description, canonical на всех страницах
4. ✅ **SSR включен** - категории и товары рендерятся на сервере
5. ✅ **Structured data** - JSON-LD schema.org для товаров и категорий

### ❌ ГЛАВНАЯ ПРОБЛЕМА:

**В sitemap.xml только 2 товара!**

```xml
<loc>https://uhti.kz/catalog/products/lol-surprise-tweens-mermaids-cleo-cove</loc>
<loc>https://uhti.kz/catalog/products/sluban-254</loc>
```

**Ожидалось:** Сотни или тысячи товаров
**Фактически:** Только 2 товара

---

## 🔍 Причины проблемы

Файл `/server/api/sitemap-routes.ts` делает запрос:

```typescript
const { data: products } = await client
  .from('products')
  .select('slug, updated_at')
  .eq('is_active', true)        // ⚠️ Только активные
  .not('slug', 'is', null)       // ⚠️ Только с slug
  .order('created_at', { ascending: false })
  .limit(10000)
```

**Возможные причины малого количества товаров:**

1. ❌ **Большинство товаров неактивны** (`is_active = false`)
2. ❌ **У товаров не заполнен `slug`** (NULL или пустая строка)
3. ❌ **В базе данных вообще мало товаров**
4. ⚠️ **RLS политика блокирует доступ** (маловероятно, но возможно)

---

## 🛠️ Пошаговое решение

### Шаг 1: Диагностика базы данных

Выполните SQL запросы из файла `/docs/SEO_CHECK_QUERIES.sql`:

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Скопируйте и выполните первый запрос:

```sql
SELECT
  COUNT(*) as total_products,
  COUNT(CASE WHEN is_active = true THEN 1 END) as active_products,
  COUNT(CASE WHEN slug IS NOT NULL AND slug != '' THEN 1 END) as products_with_slug,
  COUNT(CASE WHEN is_active = true AND slug IS NOT NULL AND slug != '' THEN 1 END) as indexable_products
FROM products;
```

**Результат покажет:**
- `total_products` - всего товаров в БД
- `active_products` - активных товаров
- `products_with_slug` - товаров с slug
- `indexable_products` - товаров, которые ДОЛЖНЫ быть в sitemap

**Если `indexable_products` = 2** → проблема подтверждена

---

### Шаг 2: Найдите проблемные товары

```sql
SELECT
  id,
  name,
  slug,
  is_active,
  created_at,
  CASE
    WHEN slug IS NULL OR slug = '' THEN 'Missing slug'
    WHEN is_active = false THEN 'Inactive'
    ELSE 'OK'
  END as issue
FROM products
WHERE (slug IS NULL OR slug = '' OR is_active = false)
LIMIT 20;
```

Это покажет товары, которые **НЕ попадают** в sitemap и почему.

---

### Шаг 3: Исправьте проблему

#### Вариант A: Активировать все товары

Если товары должны быть активны, но случайно помечены как `is_active = false`:

```sql
-- ОСТОРОЖНО! Активирует ВСЕ товары
UPDATE products
SET is_active = true
WHERE is_active = false;
```

**Проверка после выполнения:**
```sql
SELECT COUNT(*) FROM products WHERE is_active = true;
```

---

#### Вариант B: Сгенерировать slug для товаров

Если у товаров отсутствует `slug`:

```sql
-- Посмотрим товары без slug
SELECT id, name, slug
FROM products
WHERE slug IS NULL OR slug = ''
LIMIT 10;
```

**Автоматическая генерация slug** (опасно, проверьте результат!):

```sql
-- Генерируем slug из названия (транслитерация нужна вручную для кириллицы)
UPDATE products
SET slug = LOWER(
  REGEXP_REPLACE(
    REGEXP_REPLACE(name, '[^a-zA-Z0-9\s-]', '', 'g'),
    '\s+', '-', 'g'
  )
)
WHERE slug IS NULL OR slug = '';
```

**⚠️ ВАЖНО:** Этот запрос НЕ ПОДДЕРЖИВАЕТ кириллицу!
Для русских названий нужна транслитерация.

**Лучший способ:** Генерировать slug через админ-панель при создании товара.

---

#### Вариант C: Исправить вручную через админку

1. Откройте **Admin панель** → **Products**
2. Откройте каждый товар без slug
3. Заполните поле "slug" вручную (например: `igrushka-mashinka-krasnaya`)
4. Убедитесь что `is_active = true`
5. Сохраните

---

### Шаг 4: Проверка после исправления

#### 4.1. Проверьте базу данных

```sql
SELECT
  COUNT(*) as indexable_products
FROM products
WHERE is_active = true
  AND slug IS NOT NULL
  AND slug != '';
```

**Ожидаемый результат:** Должно быть много товаров (например, 100+)

---

#### 4.2. Перезапустите production build

**После изменения БД sitemap НЕ обновится автоматически!**

Если сайт на **Vercel/Netlify**:
1. Перейдите в dashboard вашего хостинга
2. Нажмите **Redeploy** или **Trigger deploy**

Если сайт на **VPS**:
```bash
# На сервере
cd /path/to/project
git pull
pnpm build
pm2 restart uhti-commerce
```

---

#### 4.3. Проверьте sitemap.xml

Откройте: https://uhti.kz/sitemap.xml

**Должны увидеть:**
```xml
<url>
  <loc>https://uhti.kz/catalog/products/igrushka-1</loc>
  <changefreq>daily</changefreq>
  <priority>0.8</priority>
</url>
<url>
  <loc>https://uhti.kz/catalog/products/igrushka-2</loc>
  <changefreq>daily</changefreq>
  <priority>0.8</priority>
</url>
<!-- ...и так далее для всех товаров -->
```

**Посчитайте количество URL:**
```bash
curl -s https://uhti.kz/sitemap.xml | grep -c "catalog/products"
```

**Ожидаемый результат:** Должно совпадать с количеством товаров в БД

---

### Шаг 5: Отправьте обновленный sitemap в Google

1. Откройте **Google Search Console**: https://search.google.com/search-console
2. Перейдите в **Sitemaps**
3. Если sitemap уже добавлен:
   - Нажмите на `https://uhti.kz/sitemap.xml`
   - Нажмите **"Перезапросить"** или **"Request indexing"**
4. Если sitemap не добавлен:
   - Введите URL: `https://uhti.kz/sitemap.xml`
   - Нажмите **"Submit"**

**Google начнет индексацию в течение 2-7 дней**

---

### Шаг 6: Ускорьте индексацию вручную (опционально)

Для **самых важных товаров** можно запросить индексацию вручную:

1. Google Search Console → **URL Inspection**
2. Введите URL товара: `https://uhti.kz/catalog/products/vash-tovar`
3. Если "URL not found" → нажмите **"Request indexing"**
4. Повторите для 10-20 главных товаров

**Лимит:** 10 запросов в день (но может быть больше)

---

## 🎯 Чеклист проверки

После выполнения всех шагов:

- [ ] **SQL запрос показывает:** `indexable_products > 2` (желательно 100+)
- [ ] **Sitemap.xml содержит:** Много товаров (проверьте количество через `grep -c`)
- [ ] **Production перезапущен** (для обновления sitemap)
- [ ] **Google Search Console:** Sitemap отправлен / переиндексирован
- [ ] **Ручная индексация:** Запрошена для топ-10 товаров
- [ ] **Через неделю:** Проверьте Google Search Console → Coverage (сколько URL проиндексировано)

---

## 📝 Дополнительные проверки

### Проблема: Категории не индексируются

Выполните аналогичные проверки для категорий:

```sql
SELECT
  COUNT(*) as total_categories,
  COUNT(CASE WHEN slug IS NOT NULL AND slug != '' THEN 1 END) as indexable_categories
FROM categories;
```

Если `indexable_categories` мало → исправьте slug в таблице `categories`

---

### Проблема: Товары есть в sitemap, но Google не индексирует

**Возможные причины:**

1. **Google еще не посетил сайт** (подождите 7-14 дней после отправки sitemap)
2. **Robots.txt блокирует** (проверьте: https://uhti.kz/robots.txt)
3. **Meta robots noindex** (проверьте source code страниц товаров)
4. **Дублированный контент** (Google игнорирует похожие страницы)
5. **Медленная загрузка** (проверьте PageSpeed Insights)

**Проверка meta robots:**

Откройте страницу товара → View Source → найдите:
```html
<meta name="robots" content="index, follow">
```

Если видите `noindex` → проблема в коде страницы (файл `/pages/catalog/products/[slug].vue`)

---

## 🚨 Критические моменты

### ⚠️ НЕ ДЕЛАЙТЕ:

1. ❌ Не удаляйте строку `Disallow: /*?*` из robots.txt
   (Она блокирует URL с фильтрами, что правильно)

2. ❌ Не меняйте приоритеты в sitemap без причины
   (Текущие приоритеты оптимальны)

3. ❌ Не генерируйте slug автоматически для кириллических названий
   (Нужна транслитерация: "Машинка" → "mashinka", а не "---")

### ✅ ДЕЛАЙТЕ:

1. ✅ Заполняйте slug при создании товара в админке
2. ✅ Активируйте товары (`is_active = true`) перед публикацией
3. ✅ Проверяйте sitemap после каждого деплоя
4. ✅ Мониторьте Google Search Console еженедельно

---

## 📚 Файлы для изучения

1. **Генерация sitemap:** `/server/api/sitemap-routes.ts`
2. **SQL запросы:** `/docs/SEO_CHECK_QUERIES.sql` ← **Выполните это сначала!**
3. **SEO конфигурация:** `/nuxt.config.ts` (строки 62-106)
4. **Robots.txt:** https://uhti.kz/robots.txt

---

## 📞 Поддержка

Если после выполнения всех шагов проблема не решается:

1. Проверьте логи Supabase на ошибки RLS
2. Проверьте логи production сервера (может быть ошибка при генерации sitemap)
3. Убедитесь что домен `uhti.kz` подтвержден в Google Search Console

---

**Дата создания:** 2026-01-09
**Автор:** Claude Code
**Статус:** КРИТИЧЕСКАЯ проблема - требует немедленного исправления
