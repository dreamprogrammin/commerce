# SEO-аудит dev.uhti.kz — 12 августа 2026

> **Обновлено 14 августа 2026.** Открытыми остались три пункта: дубли title, индексируемость превью и LCP категории — по последнему заход был, дефект найден и закрыт, но сама метрика не сдвинулась, диагноз следующего слоя записан в пункте 5. Закрыты имя автора отзыва (миграция применена на прод), зависимость блока отзывов от пагинации, контраст, realtime-подписки и CLS главной. Замеры Lighthouse переснята 14 августа — с важной оговоркой про кеш PSI, см. раздел «Цифры Lighthouse».

Состояние: что проверено, что уже починено, что осталось. Документ рассчитан на продолжение работы с другой машины.

**Сверяйте с кодом, а не только с этим файлом.** 13 августа он уже разошёлся с
репозиторием: раздел «мелкое и дешёвое» описывал как живые четыре пункта, которые
к тому моменту были закрыты, — правки пришли позже последнего обновления
документа. Прежде чем брать задачу отсюда, проверьте её в коде.

Аудит проводился по **превью-домену `dev.uhti.kz`** (сборка с ветки `dev`), потому что редизайн живёт там. Прод `uhti.kz` использовался только для сравнения — чтобы отличать привнесённое редизайном от предсуществующего.

## Чем измерялось

- Обход всех **302 URL** из sitemap с User-Agent Googlebot, разбор SSR-HTML (то, что видит краулер).
- **Lighthouse** через PageSpeed Insights v5 — по три шаблона (главная, категория, карточка) в обеих стратегиях.
- **CrUX** — полевые данные.
- Лабораторные замеры браузером с троттлингом Slow 4G + CPU ×4.

## Уже сделано

| Коммит    | Что                                                                                                                                                                                                                                             |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `9862b37` | Галерея товара не листалась свайпом на мобильных: `touch-action: pan-y` в `ProductGallery.vue` не давал браузеру обрабатывать горизонтальный жест                                                                                               |
| `519f0b8` | Вынесены листовые библиотеки из общего `vendor`-чанка: обязательный чанк 1175 → 894 КБ (−24%), `transliteration` (184 КБ) и `image-compression` (51 КБ) ушли с критического пути                                                                |
| `3f3167b` | Удалены мёртвые зависимости (`googleapis` 196 МБ, `radix-vue`, `context7`, `install`), `vue`/`vue-router` запинены на текущие версии                                                                                                            |
| `1d1822e` | **Пункт 1**: снят `<ClientOnly>` с корня карточки товара. В SSR-HTML `<h1>` 0 → 1, `<img>` 0 → 54, порядок заголовков начинается с H1. Под `ClientOnly` оставлены шесть островков, каждый помечен                                               |
| `f0e809a` | **Пункт 2**: генерация title вынесена в `utils/seoTitle.ts` (14 тестов). Средняя длина 125 → 51, в 60 знаков укладываются 172/172. Цена и материал из заголовка убраны                                                                          |
| `44c5783` | Блок отзывов на категориях заработал впервые с 6 апреля. Миграция `20260813090000_fix_category_reviews_rpc.sql` применена на прод через workflow «Миграции Supabase» 13 августа                                                                 |
| `e6fbf52` | Последняя крошка перестала быть ссылкой без адреса — теперь `<span aria-current="page">`, как в мобильной ветке. Закрывает `crawlable-anchors`                                                                                                  |
| `59b411d` | Описания категорий не обрываются посреди слова: `substring(0, 50)` заменён на `clampDescription` из нового `utils/seoDescription.ts`                                                                                                            |
| `e7e10ab` | На `/catalog` остался один заголовок первого уровня                                                                                                                                                                                             |
| `028f5ca` | У `/about` появились OG-теги                                                                                                                                                                                                                    |
| `f83832c` | Картинки для шаринга снова генерируются                                                                                                                                                                                                         |
| `d03d7ed` | Блок отзывов категории больше не зависит от страницы выдачи: показ решает сам компонент по данным всей категории, а не `categoryStats` по текущей странице                                                                                      |
| `36becb0` | Имя автора отзыва видно анонимному посетителю. `SECURITY DEFINER` с фиксированным `search_path` вместо RLS-политики: политика открыла бы строку `profiles` целиком — с телефоном, балансом и ролью. Миграция `20260813120000` применена на прод |
| `ef2ecde` | Realtime-подписки на заказы — только администратору. Прежде каждый гость открывал WebSocket на `orders` и `guest_checkouts`                                                                                                                     |
| `4161823` | Контраст текста доведён до WCAG AA. Провалов было много, а причин четыре — четыре токена темы чуть ниже порога                                                                                                                                  |
| `87f28b4` | CLS главной: скелетон ленты держится до монтирования карусели, а не снимается по таймеру. Подробности ниже                                                                                                                                      |
| `e8b8541` | Сетка каталога рендерится на сервере: карточек в разметке 0 → 12, `<img>` 8 → 33. LCP при этом не улучшился — разбор в пункте 5                                                                                                                 |

Проверено на живом превью 13 августа, запрос с UA Googlebot по карточке «Аккордеон детский HiH02»: title 41 знак (был 162), один `<h1>` в body, 18 `<img>`, порядок заголовков `h1>h2>h2>h2>h2>h3…`.

## Что осталось, по приоритету

### ~~1. Имя автора отзыва не показывается никогда~~ — закрыто

**Сделано и применено на прод 14 августа.** Миграция `20260813120000_review_author_names.sql` числится в `supabase_migrations.schema_migrations`; сверка репозитория с базой — 349 файлов против 349 записей, расхождения нет ни в одну сторону. Проверено вызовом RPC анонимным ключом: возвращаются реальные имена вместо запасного «Покупатель».

Всплыло сразу после починки блока отзывов (`44c5783`) и в самом коммите не решается.

У таблицы `profiles` **нет ни одной политики чтения для анонимов**: есть только «Users can view own profile» (`auth.uid() = id`) и «Admins can view all profiles». Обе функции отзывов объявлены `SECURITY INVOKER`, поэтому `LEFT JOIN profiles` у гостя не находит ничего, и `user_name` всегда сваливается в запасное «Покупатель».

При этом имена заполнены: на проде 9 профилей из 9 имеют `first_name`/`last_name`. То есть данные есть, их просто не отдают.

То же ограничение действует и на карточке товара: `stores/publicStore/reviewsStore.ts` подтягивает `profiles!product_reviews_profile_id_fkey(first_name, last_name, avatar_url)` тем же анонимным клиентом.

Из двух путей выбран первый:

- **`SECURITY DEFINER` с зафиксированным `search_path`** — имя отдаёт функция, таблица остаётся закрытой целиком;
- ~~RLS-политика на `profiles`~~ — отклонено. RLS открывает **строку**, а не колонку, то есть вместе с именем анониму достались бы `phone`, `telegram_chat_id`, `active_bonus_balance`, `pending_bonus_balance`, `role`. Ограничить состав колонок можно только отзывом табличного `GRANT` у роли `anon`, а он выдан на всю таблицу по умолчанию.

Решение не новое: `get_reviews_by_brand` (`20260313000001`) отдаёт имена на страницах брендов ровно так же с марта. То есть на брендах имена показывались, а на категориях и в карточке — нет; это выравнивание, а не открытие ранее закрытых данных.

Что сделано:

- `get_latest_category_reviews` → `SECURITY DEFINER` (тело не тронуто);
- новая `get_product_reviews(uuid)` для карточки — состав полей и сортировка повторяют прежний `REVIEW_SELECT`, поэтому компоненты не менялись;
- `get_reviews_by_brand` заодно получила фиксированный `search_path`: она была `DEFINER` без него с марта.

Проверено на локальной базе, приведённой к прод-состоянию этих функций. До правки аноним получал пять «Покупатель», после — реальные имена; `profiles` для анонима по-прежнему отдаёт 0 строк; немодерированный отзыв через функцию не проходит. Отдельно проверен реальный HTTP-вызов через PostgREST ключом `anon`: состав ключей совпадает с `ProductReview`, `profiles` приходит объектом, `review_images` — массивом с сортировкой по `display_order`.

### ~~2. Блок отзывов появляется в зависимости от пагинации~~ — закрыто

**Сделано**, коммит `d03d7ed`. Условие показа теперь `v-if="currentCategory"`, а решение принимает сам компонент по данным всей категории.

Было так: `categoryStats` суммировал `review_count` по `displayedProducts` — то есть только по товарам **текущей страницы выдачи**, — а `v-if` блока был завязан на `categoryStats.reviews > 0`.

Отсюда: на `/catalog/kukly-lol` и `/catalog/boys` блок есть, а на `/catalog/girls` его нет, хотя отзывы в этой категории имеются — товар с ними просто не попал на первую страницу. Смена фильтра или страницы блок то показывает, то прячет.

Сами RPC считают по всему поддереву категории и от пагинации не зависят — расходится именно условие показа.

### 3. Мелкое и дешёвое

Строки про крошки, обрезку описаний, двойной `<h1>` и OG на `/about` перенесены
в «Уже сделано» — они закрыты коммитами `e6fbf52`, `59b411d`, `e7e10ab`, `028f5ca`.
Ниже — то, что сверено с кодом и живо на 14 августа.

| Что                          | Где          | Деталь                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~Контраст текста~~          | все страницы | **Сделано**, ветка `fix/text-contrast`. Подробности ниже                                                                                                                                                                                                                                                                                                                                                                            |
| ~~CLS главной~~              | —            | **Сделано**, коммит `87f28b4`. Причина и замеры — в разделе «CLS главной» ниже                                                                                                                                                                                                                                                                                                                                                      |
| Дубли title                  | —            | Три природы. Бренд-фильтры без своей SEO-записи (`?brand=mg-toys`, `?brand=play-smart`, `?brand=soba`) наследуют title категории; родитель и потомок с одинаковым title (`/catalog/girls/kukly` и `…/kukly-dlya-devochek`); **новое** — 2 пары товаров, совпадающих в первых 48 знаках названия и различающихся цветом дальше. Последнее чинится только данными: заполнить `meta_title` этим четырём товарам, генератор его уважает |
| Превью открыт для индексации | —            | `dev.uhti.kz` отдаёт `x-robots-tag: index, follow` и robots.txt, идентичный боевому. Смягчено тем, что canonical на всех 302 страницах ведёт на `uhti.kz`, но превью правильнее закрывать заголовком `X-Robots-Tag: noindex`                                                                                                                                                                                                        |

#### Контраст текста — что оказалось на деле

«24–25 элементов на страницу» звучит как два десятка разных мест, но это не так. Прогон `axe-core` (тот же движок, которым меряет Lighthouse; лежит в `node_modules` транзитивно) показал: провалов много, а **причин четыре** — четыре токена темы, каждый чуть ниже порога. Дальше они просто размножены по карточкам товара.

| Токен                | Было      | Стало     | Худший контраст был | Стал |
| -------------------- | --------- | --------- | ------------------- | ---- |
| `--muted-foreground` | `#737373` | `#6a6a6a` | 4.04 на `#ebedf1`   | 4.61 |
| `--discount`         | `#e7000b` | `#e10003` | 4.37 на `#f5f5f5`   | 4.58 |
| `--primary`          | `#2b7fff` | `#1163df` | 3.18 на `#e3edfb`   | 4.59 |
| `--success`          | `#00c950` | `#008217` | **2.03** по белому  | 4.58 |

Значения подобраны не на глаз: светлота в OKLCH опускалась шагом 0.5 п.п. при неизменных оттенке и насыщенности, пока худший из реальных фонов не проходил 4.55:1 (запас к порогу 4.5 — axe округляет до сотых). Конвертацию делал сам Chrome через canvas, чтобы результат совпал с тем, что он потом нарисует.

`--primary` и `--success` не проходили **в обеих ролях сразу** — и текстом, и подложкой под белой надписью (белым по `#2b7fff` те же 3.76:1, по `#00c950` — 2.22:1). Притемнение чинит обе.

Отдельно нашлась пятая причина: кнопка «Каталог» в мобильной панели красилась `bg-blue-500` — сырым цветом палитры мимо токена, поэтому за темой не последовала и осталась единственным провалом на `/about` и `/brands`. Переведена на `bg-primary`.

Проверено на собранном приложении, 5 страниц × 2 ширины (390 и 1440): было 25/26/39 провалов на главной, категории и карточке — стало **0 везде**.

Чего проверка не покрывает: 14–70 элементов на страницу axe помечает как «не определено» — под ними градиент, и движок не берётся судить. Lighthouse их тоже не считает, но проблема там есть: белый текст на светлом конце градиента `LoyaltyBanner` (`#5aa0ff`) даёт **2.66:1**. Это отдельная задача — красить надо не токен, а сам градиент. Тёмная тема не проверялась: переключателя в приложении нет, `.dark`-токены недостижимы.

### ~~4. Анонимный посетитель подписывается на каналы заказов~~ — закрыто

**Сделано**, ветка `fix/realtime-guest-subscriptions`. Только фронт, миграции не нужны.

`app.vue` звал `subscribeAll()` из `useOrderRealtime` безусловно — без проверки роли и факта авторизации. На каждой странице у каждого гостя открывался WebSocket и две подписки `postgres_changes` на `orders` и `guest_checkouts`.

**Поправка к первой редакции пункта.** Там было написано, что в консоль летит `❌ Error subscribing to orders channel: undefined`. В живом браузере на `dev.uhti.kz` этого нет: канал отвечает `phx_reply status: ok`, приходит `system: Subscribed to PostgreSQL`, в консоли `✅ Subscribed to orders channel`. Ошибка была артефактом песочницы PageSpeed — там же рядом висел `ERR_NAME_NOT_RESOLVED` на самом WebSocket, про который в первой редакции честно стояло «не уверен». Проверено в живом браузере, как и предполагалось: подписка не падает.

То есть чинилась не ошибка, а бесполезная работа. Событий гость не получал никогда: RLS у `orders` и `guest_checkouts` не отдаёт анониму ни одной строки (REST на обе таблицы отвечает `[]`), а `postgres_changes` применяет ту же RLS к каждому подписчику. Соединение висело, событие не приходило.

Теперь подписка открывается только при `profileStore.isAdmin`, и не разово на `mount`, а по `watch`: профиль подгружается отложенно, и на момент монтирования роль неизвестна даже у вошедшего администратора.

Проверено на собранном приложении: у гостя **0 WebSocket-соединений** и ни одного события каналов, было — 1 соединение и 2 подписки. Путь администратора в браузере **не проверен**: вход через Google OAuth агенту недоступен, покрыт 4 юнит-тестами.

**Замечание в сторону.** Задумка композабла — «обновить остатки, когда заказ подтверждён» — для покупателей не работала никогда по той же причине (RLS). Эта правка её не чинит, а убирает цену. Если остатки должны обновляться у покупателей, это другая конструкция: broadcast-канал или realtime на самой таблице `products`.

### 5. LCP категории — 11.5 секунды на мобиле

Самая тяжёлая метрика сайта. Порог Google — 2.5 с. **Пункт открыт: заход
14 августа результата по метрике не дал**, хотя закрыл по дороге отдельный
настоящий дефект (см. ниже).

#### Что было найдено и закрыто: сетка не рендерилась на сервере

Трасса на превью (390px, Slow 4G, CPU ×4) до правки:

```
TTFB          480 мс   сервер быстрый
FCP          4424 мс   первым красится фильтр-чип, не товары
JS 273 КБ    3245 мс + 3164 мс на скачивание и разбор
RPC старт    7931 мс   get_filtered_products уходит только теперь
товары      ~9980 мс
картинки    10358 мс
LCP         12044 мс   картинка товара
```

Название LCP-товара встречалось в HTML шесть раз и **ни разу вне `<script>`** —
только в payload. Карточек в разметке 0, скелетонов 68.

Причин было две, обе сняты коммитом `e8b8541`:

1. **SSR-префетч в `useCatalogQuery` не работал.** Стоял `useAsyncData` без
   `await`, а `ssrData.value` читался следующей строкой — синхронно, до
   разрешения промиса. `setQueryData` не вызывался ни разу.
2. **Сетка лежала под `<ClientOnly>`** с `ProductGridSkeleton` в `#fallback`.

Порядок вызовов в композабле принципиален, оба неверных варианта проверены
запуском:

- `await` перед `useQuery` роняет страницу в 500: внутри вызывается
  `useQueryClient()`, а после ожидания контекст инъекции потерян;
- посев кеша **после** `useQuery` не даёт ничего: на сервере vue-query
  не подписывает наблюдателя на кеш.

Рабочий порядок: клиент берём синхронно, ждём данные, сеем кеш, затем создаём
`useQuery` с явно переданным `queryClient`.

Результат на превью: карточек в разметке 0 → **12**, `<img>` 8 → **33**,
гидратация без предупреждений на 390px и 1440px, CLS категории 0.0004.
Для краулера это полноценная страница вместо скелетонов.

#### Почему LCP при этом не улучшился

**12.0 → 11.5 с, то есть в пределах шума.** Картинки стали стартовать раньше
(7.7 с вместо 10.3 с), но выигрыш съеден тем, что сама разметка приходит
только к 7.6 с. Замер после правки:

```
HTML полностью получен  7665 мс
скрипты стартуют        3859 мс  — больше 20 файлов параллельно
самый крупный чанк      273 КБ, докачивается к 8272 мс
```

Дело не в весе разметки: сжатой она 182 КБ, это меньше секунды на Slow 4G.
Дело в том, что на 3.9-й секунде браузер находит в `<head>` два десятка
`modulepreload` и они забирают канал ровно тогда, когда нужен HTML.

Вторая цифра в ту же сторону: из 691 КБ несжатого HTML **507 КБ — это payload
Nuxt**, а собственно разметки 159 КБ. Данные каталога уезжают дважды —
в разметке и в payload.

#### Рычаги на следующий заход

1. **Урезать payload.** 507 КБ на страницу — данные, которые уже есть
   в разметке. Самый весомый рычаг из трёх.
2. **Проредить `modulepreload`.** Два десятка файлов в голове забивают канал
   в самый неподходящий момент.
3. **Preload LCP-картинки**, чтобы первая карточка не ждала конца разметки.

### CLS главной: причина и правка

Закрыто коммитом `87f28b4`. Разбор стоит того, чтобы остаться в документе:
похожая конструкция есть и на других страницах.

Замер на превью (390px, Slow 4G, CPU ×4, атрибуция через `PerformanceObserver`)
показал: сдвигов четыре, но два дают 0.2427 из 0.244. Двигался один блок —
«Популярные категории»:

```
4964 мс  y=1213        скелетон держит место
5739 мс  y= 707  -506  скелетон снят, на его месте пусто
6574 мс  y=1225  +518  карусель наконец отрисовалась
```

Дёргал **не** переход «скелетон → карусель», а дыра между ними. Скелетон
снимался условием `!shouldRenderLowerBlocks`, а это не состояние загрузки,
а `setTimeout` на 1000 мс (`pages/index.vue`). Карусели ленивые
(`defineAsyncComponent`), и к моменту снятия скелетона их чанк ещё грузился —
блок схлопывался на ~800 мс и разворачивался обратно. Промежуток между
сдвигами, 835 мс, ровно об этом.

Теперь скелетон снимается по `@vue:mounted` самой карусели, а не по таймеру.

Проверено одним и тем же трейсом на локальной сборке, до и после:
без правки CLS 0.3119 со скачком −506, с правкой 0.1921 и скачка нет
(заголовок смещается на +12). Остаток 0.19 — локальный артефакт: бакеты
storage на локальной базе пусты, картинки не приходят, высоты плывут.
На превью этого нет.

**Не проверено:** цифра на превью. Сборка `87f28b4` на момент написания
не выкачена — check-suite Vercel завис в статусе `queued`, при том что
GitHub событие доставил, проект не на паузе, лимиты не исчерпаны и
инцидентов на платформе нет. Перемерять после выкатки.

### Не является багом

- **`Clean-param: brands` в robots против `?brand=` в URL.** Параметров действительно два: `brands` — мультифильтр (`[...slug].vue:954`), `brand` — SEO-лендинги категория+бренд (строка 951). Первый чистится, второй намеренно оставлен индексируемым. Проверено, всё верно.
- **`FAQPage` на карточках и бренд-страницах.** Google отключил для него сниппеты 7 мая 2026 — вреда нет, просто больше ничего не даёт. Удалять не нужно.
- **`googleapis` в `pages/index.vue`** — это `fonts.googleapis.com` в `preconnect`, не библиотека.
- **Концевые числа в названиях товаров.** Выглядят огрызками, но 17 из 24 — модели и артикулы (Ferrari 488, MOKA 2075, Mermaze 580812), по которым ищут. Правило по их отбрасыванию рассматривалось и отклонено.

## Цифры Lighthouse

Замеры 14 августа сделаны на сборке `cc399d6` — со всеми правками, кроме CLS
(`87f28b4` на момент замера ещё не был выкачен, см. оговорку в конце раздела).

| Страница  |         | Perf 12.08 | Perf 14.08 | LCP        | FCP       | TBT       | CLS   | A11y | SEO | BP  |
| --------- | ------- | ---------- | ---------- | ---------- | --------- | --------- | ----- | ---- | --- | --- |
| Главная   | моб.    | 47         | **77**     | 5.8 → 3.1  | 4.7 → 2.3 | 240 → 82  | 0.249 | 100  | 100 | 100 |
|           | десктоп | 95         | 90         | 0.7 → 1.0  | 0.4 → 0.8 | 40 → 57   | 0.124 | 100  | 100 | 100 |
| Категория | моб.    | 49         | 50         | 8.7 → 10.0 | 4.4 → 3.9 | 350 → 347 | 0.09  | 100  | 100 | 100 |
|           | десктоп | 77         | **83**     | 1.9 → 1.8  | 0.8 → 0.8 | 190 → 107 | 0.055 | 100  | 100 | 100 |
| Карточка  | моб.    | 61         | 62         | 6.3 → 5.9  | 4.8 → 4.7 | 140 → 142 | 0     | 98   | 100 | 100 |
|           | десктоп | 95         | 94         | 1.3 → 1.2  | 0.8 → 0.9 | 50 → 109  | 0.025 | 99   | 100 | 100 |

### Оговорка: медианы не получились

Замер задумывался как медиана пяти прогонов на мобиле и трёх на десктопе —
именно потому, что одиночный прогон PSI тут гуляет. Не вышло: разброс по
прогонам оказался `[77,77,77,77,77]`, `[50,50,50,50,50]`, `[83,83,83]`.
Пять одинаковых значений подряд — это не стабильность, а **кеш PSI**: пауза
между запросами была 2 секунды, и API отдавал сохранённый результат.

Настоящий разброс видно там, где кеш промахнулся: главная на десктопе дала
`[80, 93, 90]` — 13 баллов.

**Читать таблицу надо как один замер, а не как медиану.** Чтобы медиана
заработала, между прогонами нужна пауза в минуты, а не в секунды.

### Где работа окупилась — не в Performance

| Категория               | 12.08    | 14.08                          |
| ----------------------- | -------- | ------------------------------ |
| Accessibility           | 96/96/94 | **100/100/98**                 |
| SEO (карточка, десктоп) | 92       | **100**                        |
| Best Practices          | 92       | **100** на всех шести прогонах |

Accessibility — правка контраста (`4161823`). SEO на карточке — крошки
(`e6fbf52`). Best Practices 92 → 100 совпадает по времени с починкой отзывов
и realtime-подписок: до них в консоли висели 404 на RPC и ошибки подписки,
а `errors-in-console` — это проверка именно Best Practices. Причинность
не доказывалась, но сходится.

По Performance крупный сдвиг ровно один — главная на мобиле: 47 → 77,
LCP вдвое быстрее, TBT втрое меньше.

### Худшее место теперь — LCP категории

**10.0 с на мобиле**, было 8.7. Единственная метрика, которая за два дня
не сдвинулась ни на шаг, и единственная, которая ухудшилась. Категория —
основная посадочная страница из поиска. Открытый пункт, см. список выше.

Замеры делались до выкатки CLS-фикса `87f28b4`, поэтому CLS главной в таблице
всё ещё 0.249. Ожидаемое значение после выкатки — см. раздел ниже.

## CrUX: полевых данных нет

Ни по превью, ни **по проду**:

> No CrUX data for this origin. The site likely has insufficient Chrome traffic volume for eligibility.

`uhti.kz` не набирает порога трафика Chrome. Практический вывод: полевые Core Web Vitals сейчас не влияют на ранжирование сайта — у Google их по этому домену просто нет. Ориентироваться приходится на лабораторные.

## Как повторить замеры

Настройка окружения (Python-рантайм, Chromium, ключ Google) — в [CLAUDE_SETUP.md](./CLAUDE_SETUP.md).

Те же три URL, что и 13 августа:

```bash
claude-seo run pagespeed_check.py https://dev.uhti.kz/ --strategy both --json
claude-seo run pagespeed_check.py https://dev.uhti.kz/catalog/boys --strategy both --json
claude-seo run pagespeed_check.py https://dev.uhti.kz/catalog/products/akkordeon-detskiy-hih02-plastik-zvukovye-effekty-yarkiy-dizayn-dlya-detey-ot-3-let --strategy both --json

# только полевые данные
claude-seo run pagespeed_check.py https://uhti.kz/ --crux-only --json

# разбор одной страницы
claude-seo run parse_html.py --url https://dev.uhti.kz/ --json
```

Нужные поля в JSON лежат в `psi.mobile.lighthouse_scores`, `psi.mobile.lab_metrics`, а консольные ошибки — в `psi.mobile.audit_details["errors-in-console"].items`.

Обход всего sitemap с подсчётом мета-тегов и заголовков делался разовым скриптом на venv плагина (`~/.local/share/claude-seo/.venv/bin/python`, там есть `requests`, `bs4`, `lxml`). Суть: забрать `<loc>` из `https://dev.uhti.kz/sitemap.xml`, подменить хост на превью (в sitemap лежат канонические адреса `uhti.kz`), обойти пулом в 6 потоков с UA Googlebot и посчитать по каждому URL `title`/`description`/число `<h1>`/`canonical`, отдельно помечая описания, попавшие под срез: `re.match(r"^.{50} \(", desc)`.

## Чего аудит не покрыл

- **Search Console, позиции, трафик, индексация** — нужен сервисный аккаунт Google, настроен только API-ключ.
- **Страницы за логином** (`/profile/**`, вся админка) — вход через Google OAuth агенту недоступен. Это же главный непроверенный участок после выноса чанков: `transliteration`, `lodash`, `vue-table` и `image-compression` уехали в отдельные чанки, и если что-то сломалось, то именно в админке — стоит открыть загрузку картинок товара и генерацию slug.
- **Страница успешного заказа** (`canvas-confetti`) — нужен реально оформленный заказ.
- **Бренд-страницы и статика через Lighthouse** — гонялись только три шаблона.
- **Реальные устройства** — только эмуляция Chromium.
- **Повторный обход всех 302 URL** после фиксов 13 августа — переснят только Lighthouse по трём шаблонам плюс точечные проверки `/catalog`, `/about`, трёх категорий и одной карточки.
