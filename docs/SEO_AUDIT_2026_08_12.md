# SEO-аудит dev.uhti.kz — 12 августа 2026

Состояние на конец дня: что проверено, что уже починено, что осталось. Документ рассчитан на продолжение работы с другой машины.

Аудит проводился по **превью-домену `dev.uhti.kz`** (сборка с ветки `dev`), потому что редизайн живёт там. Прод `uhti.kz` использовался только для сравнения — чтобы отличать привнесённое редизайном от предсуществующего.

## Чем измерялось

- Обход всех **302 URL** из sitemap с User-Agent Googlebot, разбор SSR-HTML (то, что видит краулер).
- **Lighthouse** через PageSpeed Insights v5 — по три шаблона (главная, категория, карточка) в обеих стратегиях.
- **CrUX** — полевые данные.
- Лабораторные замеры браузером с троттлингом Slow 4G + CPU ×4.

## Уже сделано

| Коммит    | Что                                                                                                                                                                              |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `9862b37` | Галерея товара не листалась свайпом на мобильных: `touch-action: pan-y` в `ProductGallery.vue` не давал браузеру обрабатывать горизонтальный жест                                |
| `519f0b8` | Вынесены листовые библиотеки из общего `vendor`-чанка: обязательный чанк 1175 → 894 КБ (−24%), `transliteration` (184 КБ) и `image-compression` (51 КБ) ушли с критического пути |
| `3f3167b` | Удалены мёртвые зависимости (`googleapis` 196 МБ, `radix-vue`, `context7`, `install`), `vue`/`vue-router` запинены на текущие версии                                             |

## Что осталось, по приоритету

### 1. Карточка товара не рендерится на сервере

**172 из 172** карточек не имеют в SSR-HTML ни `<h1>`, ни единого `<img>`; название товара присутствует только в `<head>`.

Причина: весь шаблон обёрнут в `<ClientOnly>` — `pages/catalog/products/[slug].vue:1115`, а `<h1 class="pdp-title">` лежит внутри, на строке 1161.

Последствия шире, чем отсутствие заголовка:

- LCP-элемент карточки — это и есть `H1.pdp-title`, он появляется только после гидратации. Lighthouse mobile: **LCP 6.3 с**.
- Провалена проверка «Heading elements are not in a sequentially-descending order» — разметка начинается сразу с H3.

**Предсуществующее, не от редизайна:** прод отдаёт ровно то же самое. Проверено сравнением обоих доменов.

### 2. Длина title карточек

Все **172** длиннее 60 символов, средняя **125**, максимум 162. В выдаче обрезается больше половины.

Плюс дублирование материала: «Аккордеон детский HiH02 **пластик** — … **пластик** — от 3 690 ₸». Материал уже есть в названии товара, а генератор приклеивает его второй раз — `pages/catalog/products/[slug].vue:485-488`.

### 3. Мелкое и дешёвое

| Что                                       | Где                                    | Деталь                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Крошки как `<a>` без `href`               | `components/global/Breadcrumbs.vue:62` | Десктопная ветка оборачивает **все** крошки, включая последнюю, у которой `href` не задан (`[slug].vue:207`). Lighthouse: «Links are not crawlable», SEO 92 на десктопе. На мобиле проверка проходит только потому, что крошки скрыты (`hidden lg:block`). **Мобильная ветка того же файла (строки 111–116) делает правильно — через `<span>`** |
| Описания категорий обрезаны посреди слова | `pages/catalog/[...slug].vue:1097`     | `substring(0, 50)` без проверки границы слова. Задето 3 категории: `/catalog/boys` («…транспорт **дл**»), `/catalog/constructors-root` («…широкий **выбо**»), `/catalog/kiddy`. В соседнем блоке (строки 1101–1108) обрезка сделана по последнему пробелу — образец рядом                                                                       |
| Контраст текста                           | все страницы                           | Единственный провал Accessibility, и на мобиле, и на десктопе                                                                                                                                                                                                                                                                                   |
| CLS главной                               | —                                      | 0.249 при пороге 0.1 (лабораторно 0.139). На категории и карточке смещений нет                                                                                                                                                                                                                                                                  |
| Дубли title                               | —                                      | 4 группы. Две природы: бренд-фильтры без своей SEO-записи (`?brand=mg-toys`, `?brand=play-smart`, `?brand=soba`) наследуют title категории; родитель и потомок с одинаковым title (`/catalog/girls/kukly` и `…/kukly-dlya-devochek`)                                                                                                            |
| Превью открыт для индексации              | —                                      | `dev.uhti.kz` отдаёт `x-robots-tag: index, follow` и robots.txt, идентичный боевому. Смягчено тем, что canonical на всех 302 страницах ведёт на `uhti.kz`, но превью правильнее закрывать заголовком `X-Robots-Tag: noindex`                                                                                                                    |
| `/catalog`                                | —                                      | два `<h1>` на странице                                                                                                                                                                                                                                                                                                                          |
| `/about`                                  | —                                      | нет OG-тегов, при шаринге карточка пустая                                                                                                                                                                                                                                                                                                       |

### Не является багом

- **`Clean-param: brands` в robots против `?brand=` в URL.** Параметров действительно два: `brands` — мультифильтр (`[...slug].vue:954`), `brand` — SEO-лендинги категория+бренд (строка 951). Первый чистится, второй намеренно оставлен индексируемым. Проверено, всё верно.
- **`FAQPage` на карточках и бренд-страницах.** Google отключил для него сниппеты 7 мая 2026 — вреда нет, просто больше ничего не даёт. Удалять не нужно.
- **`googleapis` в `pages/index.vue`** — это `fonts.googleapis.com` в `preconnect`, не библиотека.

## Цифры Lighthouse (12.08.2026)

| Страница  |         | Perf | LCP   | FCP   | TBT    | CLS   |
| --------- | ------- | ---- | ----- | ----- | ------ | ----- |
| Главная   | моб.    | 47   | 5.8 с | 4.7 с | 240 мс | 0.249 |
|           | десктоп | 95   | 0.7 с | 0.4 с | 40 мс  | 0.124 |
| Категория | моб.    | 49   | 8.7 с | 4.4 с | 350 мс | 0.09  |
|           | десктоп | 77   | 1.9 с | 0.8 с | 190 мс | 0.055 |
| Карточка  | моб.    | 61   | 6.3 с | 4.8 с | 140 мс | 0     |
|           | десктоп | 95   | 1.3 с | 0.8 с | 50 мс  | 0.002 |

**Десктоп в порядке, проблема целиком на мобильных.** FCP 4.4–4.8 с на всех трёх страницах при быстром TTFB — время съедает не сервер, а разбор JS.

Замеры сделаны **до** выноса чанков (`519f0b8`). Их стоит повторить, когда сборка окажется на превью.

> Баллы PSI mobile заметно гуляют от прогона к прогону: на главной в тот же день получались и 47, и 73. Для устойчивой цифры нужно 3–5 прогонов и медиана.

## CrUX: полевых данных нет

Ни по превью, ни **по проду**:

> No CrUX data for this origin. The site likely has insufficient Chrome traffic volume for eligibility.

`uhti.kz` не набирает порога трафика Chrome. Практический вывод: полевые Core Web Vitals сейчас не влияют на ранжирование сайта — у Google их по этому домену просто нет. Ориентироваться приходится на лабораторные.

## Как повторить замеры

Настройка окружения (Python-рантайм, Chromium, ключ Google) — в [CLAUDE_SETUP.md](./CLAUDE_SETUP.md).

```bash
# Lighthouse + CrUX по одной странице
claude-seo run pagespeed_check.py https://dev.uhti.kz/ --strategy both --json

# только полевые данные
claude-seo run pagespeed_check.py https://uhti.kz/ --crux-only --json

# разбор одной страницы
claude-seo run parse_html.py --url https://dev.uhti.kz/ --json
```

Обход всего sitemap с подсчётом мета-тегов и заголовков делался разовым скриптом на venv плагина (`~/.local/share/claude-seo/.venv/bin/python`, там есть `requests`, `bs4`, `lxml`). Суть: забрать `<loc>` из `https://dev.uhti.kz/sitemap.xml`, подменить хост на превью, обойти пулом в 6 потоков с UA Googlebot и посчитать по каждому URL `title`/`description`/число `<h1>`/`canonical`, отдельно помечая описания, попавшие под срез: `re.match(r"^.{50} \(", desc)`.

## Чего аудит не покрыл

- **Search Console, позиции, трафик, индексация** — нужен сервисный аккаунт Google, настроен только API-ключ.
- **Страницы за логином** (`/profile/**`, вся админка) — вход через Google OAuth агенту недоступен. Это же главный непроверенный участок после выноса чанков: `transliteration`, `lodash`, `vue-table` и `image-compression` уехали в отдельные чанки, и если что-то сломалось, то именно в админке — стоит открыть загрузку картинок товара и генерацию slug.
- **Страница успешного заказа** (`canvas-confetti`) — нужен реально оформленный заказ.
- **Бренд-страницы и статика через Lighthouse** — гонялись только три шаблона.
- **Реальные устройства** — только эмуляция Chromium.
