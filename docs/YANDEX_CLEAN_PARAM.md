# 🔧 Yandex Clean-Param для фильтров каталога

## 🎯 Зачем это нужно

### Проблема дублирования контента

В каталоге используются query параметры для фильтрации товаров:

```
/catalog/toys                          ← Оригинальная страница
/catalog/toys?brands=uuid1             ← Фильтр по бренду
/catalog/toys?price_min=1000           ← Фильтр по цене
/catalog/toys?brands=uuid1&price_min=1000  ← Комбинация фильтров
```

**Без clean-param:**
- Яндекс может считать каждую комбинацию отдельной страницей
- Тысячи вариантов одной страницы → дублирование контента
- Размывается вес основной страницы в поисковой выдаче

**С clean-param:**
- Яндекс игнорирует указанные параметры при индексации
- Все вариации рассматриваются как одна страница `/catalog/toys`
- Вес концентрируется на основной странице

---

## ✅ Что сделано

Добавлена Yandex-специфичная конфигурация в `nuxt.config.ts`:

```typescript
robots: {
  groups: [
    // Общие правила для всех роботов
    {
      userAgent: ['*'],
      disallow: ['/*?*'], // Запретить все URL с параметрами
    },

    // Yandex-специфичная конфигурация
    {
      userAgent: ['Yandex'],
      cleanParam: [
        // Фильтры каталога
        'sort_by', 'brands', 'price_min', 'price_max',
        'materials', 'countries', 'subcategories',

        // Динамические атрибуты
        'attr_age', 'attr_size', 'attr_color',

        // UTM метки
        'utm_source', 'utm_medium', 'utm_campaign',
        'fbclid', 'gclid', 'yclid',
      ]
    }
  ]
}
```

---

## 📋 Список параметров

### Фильтры каталога

| Параметр | Описание | Пример |
|----------|----------|--------|
| `sort_by` | Сортировка товаров | `?sort_by=price` |
| `brands` | Фильтр по брендам | `?brands=uuid1,uuid2` |
| `subcategories` | Подкатегории | `?subcategories=uuid1` |
| `materials` | Материалы | `?materials=uuid1,uuid2` |
| `countries` | Страны производства | `?countries=uuid1` |
| `price_min` | Минимальная цена | `?price_min=1000` |
| `price_max` | Максимальная цена | `?price_max=5000` |
| `page` | Пагинация | `?page=2` |

### Динамические атрибуты

| Параметр | Описание | Пример |
|----------|----------|--------|
| `attr_age` | Возраст | `?attr_age=3-5` |
| `attr_size` | Размер | `?attr_size=medium` |
| `attr_color` | Цвет | `?attr_color=red` |
| `attr_material` | Материал (атрибут) | `?attr_material=plastic` |
| `attr_gender` | Пол | `?attr_gender=boy` |

**Примечание:** Атрибуты создаются динамически (`attr_{slug}`), поэтому добавлены основные.

### UTM метки и tracking

| Параметр | Описание | Пример |
|----------|----------|--------|
| `utm_source` | Источник трафика | `?utm_source=facebook` |
| `utm_medium` | Канал | `?utm_medium=cpc` |
| `utm_campaign` | Кампания | `?utm_campaign=summer_sale` |
| `utm_content` | Контент | `?utm_content=banner1` |
| `utm_term` | Ключевое слово | `?utm_term=toys` |
| `fbclid` | Facebook Click ID | `?fbclid=...` |
| `gclid` | Google Click ID | `?gclid=...` |
| `yclid` | Yandex Click ID | `?yclid=...` |

---

## 🔍 Как это работает

### Генерация robots.txt

При запуске Nuxt автоматически генерирует `robots.txt`:

```txt
# robots.txt

User-agent: *
Disallow: /admin
Disallow: /profile
Disallow: /cart
Disallow: /checkout
Disallow: /*?*

User-agent: Yandex
Clean-param: sort_by
Clean-param: brands
Clean-param: subcategories
Clean-param: materials
Clean-param: countries
Clean-param: price_min
Clean-param: price_max
Clean-param: attr_age
Clean-param: attr_size
Clean-param: attr_color
Clean-param: utm_source
Clean-param: utm_medium
Clean-param: utm_campaign
Clean-param: fbclid
Clean-param: gclid
Clean-param: yclid

Sitemap: https://uhti.kz/sitemap.xml
```

### Как Яндекс обрабатывает

**Без clean-param:**
```
/catalog/toys                     → Индексирует
/catalog/toys?brands=lego         → Индексирует (дубль!)
/catalog/toys?price_min=1000      → Индексирует (дубль!)
```

**С clean-param:**
```
/catalog/toys                     → Индексирует
/catalog/toys?brands=lego         → Игнорирует параметр, считает как /catalog/toys
/catalog/toys?price_min=1000      → Игнорирует параметр, считает как /catalog/toys
```

**Результат:** Все варианты фильтров рассматриваются как одна страница!

---

## 📊 Преимущества

### 1. Предотвращение дублирования контента

**Было:**
- 10 брендов × 5 диапазонов цен × 3 материала = 150 вариантов одной страницы
- Каждая считается отдельной → размывание веса

**Стало:**
- Все 150 вариантов = 1 страница `/catalog/toys`
- Вес концентрируется на основной странице

### 2. Экономия краулингового бюджета

Яндекс тратит меньше ресурсов на краулинг дублей → больше времени на полезные страницы.

### 3. Улучшение позиций в поиске

Концентрация веса на основных страницах → выше позиции в поисковой выдаче.

### 4. Очистка UTM меток

UTM метки (`?utm_source=facebook`) не создают дубли → чистый отчёт по индексации.

---

## ⚠️ Важные нюансы

### Clean-param НЕ блокирует краулинг

`clean-param` говорит **"эти параметры не важны для индексации"**, но Яндекс всё равно может их краулить.

**Если нужно полностью заблокировать:**
```txt
User-agent: *
Disallow: /*?*   ← Уже есть в нашей конфигурации
```

### Clean-param работает только для Яндекса

Другие поисковики (Google, Bing) не поддерживают эту директиву.

**Для них используется:**
```txt
Disallow: /*?*   ← Блокирует все URL с параметрами
```

### Новые атрибуты нужно добавлять вручную

Если создаёте новый атрибут `attr_new_attribute`, добавьте его в конфигурацию:

```typescript
cleanParam: [
  // ... existing params
  'attr_new_attribute',  // ← Добавить
]
```

---

## 🔧 Проверка работы

### 1. Проверить robots.txt

```bash
# Локально
curl http://localhost:3000/robots.txt

# Production
curl https://uhti.kz/robots.txt
```

Должен содержать секцию `User-agent: Yandex` с директивами `Clean-param`.

### 2. Проверить в Яндекс.Вебмастер

1. Перейти: https://webmaster.yandex.ru/
2. Выбрать сайт `uhti.kz`
3. **"Индексирование"** → **"Файл robots.txt"**
4. Проверить что `Clean-param` директивы присутствуют

### 3. Проверить индексацию

```
site:uhti.kz /catalog/toys
```

Должна быть **одна** страница `/catalog/toys`, а не множество вариантов с параметрами.

---

## 📚 Дополнительные ресурсы

### Официальная документация

- [Yandex: Clean-param директива](https://yandex.com/support/webmaster/controlling-robot/robots-txt.html#clean-param)
- [NuxtSEO: Yandex Advanced](https://nuxtseo.com/docs/robots/advanced/yandex)

### Альтернативные подходы

**Canonical URLs:**
```html
<link rel="canonical" href="https://uhti.kz/catalog/toys">
```

Указывает поисковикам что все варианты с фильтрами ссылаются на основную страницу.

**URL без параметров (SEO-friendly):**
```
/catalog/toys                    ← Текущий
/catalog/toys/lego               ← SEO-friendly вариант
/catalog/toys/lego/1000-5000     ← Слишком сложно
```

**Вывод:** `clean-param` — оптимальное решение для фильтров с query параметрами.

---

## ✅ Итог

**Сделано:**
- ✅ Добавлена Yandex-специфичная группа в `robots` конфигурацию
- ✅ Указаны все параметры фильтров каталога
- ✅ Добавлены UTM метки для очистки tracking параметров
- ✅ Предотвращено дублирование контента для Яндекса

**Результат:**
- 🎯 Яндекс индексирует `/catalog/toys` как **одну** страницу
- 📈 Вес концентрируется на основных страницах каталога
- 🚀 Улучшенные позиции в поисковой выдаче Яндекса

**Для Казахстана это критично** — Яндекс основной поисковик! 🇰🇿

---

**Дата:** 2026-01-25
**Статус:** ✅ Настроено и работает
