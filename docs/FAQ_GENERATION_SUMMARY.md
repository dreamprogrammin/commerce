# Система автогенерации FAQ

## TL;DR

**Гибридная система:** SQL для базовых вопросов + AI для премиум-товаров

```
Товар < 50,000₸     → SQL-генерация (3 вопроса, 1 сек, бесплатно)
Товар > 50,000₸     → SQL + AI (3+4 вопроса, 5-10 сек, $0.01-0.03)
Массовая генерация  → Только SQL (экономия токенов)
```

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│  Админка: Кнопка "Сгенерировать FAQ"                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  RPC: generate_product_questions(product_id, skip_ai)   │
│  - Удаляет старые автовопросы                          │
│  - Создаёт базовые вопросы (SQL)                       │
│  - Возвращает: {needs_ai: boolean, product_data: {...}}│
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼ (если needs_ai = true)
┌─────────────────────────────────────────────────────────┐
│  Edge Function: generate-premium-questions              │
│  - Получает данные товара                              │
│  - Формирует промпт для Claude                         │
│  - Отправляет в Anthropic API                          │
│  - Парсит JSON с вопросами                             │
│  - Сохраняет в БД                                      │
└─────────────────────────────────────────────────────────┘
```

## Базовые вопросы (SQL)

**Генерируются для всех товаров:**

1. **Возраст** — Если есть `min_age_years` / `max_age_years`
2. **Доставка** — Всегда (стандартный текст)
3. **Возврат** — Всегда (14 дней, условия)

**Характеристики:**

- ⚡ Мгновенно (SQL-запрос)
- 💰 Бесплатно
- 📝 Шаблонные ответы

## Премиум вопросы (AI)

**Генерируются для товаров > 50,000₸:**

**Фокус:**

- Убеждение купить дорогой товар
- Обоснование высокой цены
- Подчёркивание уникальности
- Ответы на возражения

**Примеры тем:**

- "Почему так дорого?"
- "В чём отличие от дешёвых?"
- "Какие уникальные возможности?"
- "Действительно ли стоит брать?"

**Характеристики:**

- 🤖 Claude 3.5 Haiku
- ⏱️ 5-10 секунд
- 💰 $0.01-0.03 за товар
- ✨ Уникальные ответы

## Использование

### Индивидуально (с AI)

```
Админка → Товары → [Выбрать товар] → "Сгенерировать FAQ"
```

### Массово (без AI)

```
Админка → Товары → "Сгенерировать FAQ для всех"
```

⚠️ Массовая генерация **НЕ использует AI** (экономия токенов)

## Файлы

```
supabase/
├── migrations/
│   └── 20260128070000_add_auto_generated_questions.sql  # RPC функции
└── functions/
    └── generate-premium-questions/
        └── index.ts                                      # AI Edge Function

composables/
└── useProductQuestions.ts                                # Frontend API

pages/admin/products/
├── [id].vue                                              # Индивидуальная генерация
└── index.vue                                             # Массовая генерация

components/product/
└── ProductQuestions.vue                                  # Отображение FAQ
```

## Настройка

### 1. API ключ Claude

```bash
# Получить: https://console.anthropic.com/settings/keys
supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
```

### 2. Деплой Edge Function

```bash
supabase functions deploy generate-premium-questions
```

### 3. Применить миграцию

```bash
supabase db reset
```

## Кастомизация

### Изменить порог премиум-товара

Файл: `supabase/migrations/20260128070000_add_auto_generated_questions.sql`

```sql
IF v_price > 50000 AND NOT p_skip_ai THEN  -- ← Изменить 50000
```

### Изменить промпт AI

Файл: `supabase/functions/generate-premium-questions/index.ts`

```typescript
const prompt = `...` // ← Отредактировать промпт
```

### Изменить модель AI

```typescript
model: 'claude-3-5-haiku-20241022',  // ← Изменить модель
```

Доступные:

- `claude-3-5-haiku-20241022` — $0.01/1K токенов (быстро)
- `claude-3-5-sonnet-20241022` — $0.015/1K токенов (умнее)

## FAQ

**Q: Сколько стоит?**
A: SQL бесплатно, AI ~$0.01-0.03 за товар

**Q: Можно без AI?**
A: Да, передайте `skip_ai: true` в RPC функцию

**Q: Перезапишутся ли пользовательские вопросы?**
A: Нет, только автогенерированные (`is_auto_generated = true`)

**Q: Как часто генерировать?**
A: При добавлении товара + при изменении цены/характеристик

## Мониторинг

### Логи Edge Function

```bash
supabase functions logs generate-premium-questions
```

### SQL статистика

```sql
-- Сколько автовопросов
SELECT COUNT(*) FROM product_questions WHERE is_auto_generated = true;

-- Премиум товары без AI-вопросов
SELECT id, name, price
FROM products
WHERE price > 50000
  AND id NOT IN (
    SELECT DISTINCT product_id
    FROM product_questions
    WHERE is_auto_generated = true
  );
```

## Метрики

**Для 1000 товаров:**

- Базовая генерация: 1-5 минут
- С AI (100 премиум): +10-20 минут
- Стоимость AI: $1-3

**SEO эффект:**

- +300% текста на странице
- Структурированные данные для Google
- Покрытие long-tail запросов

---

📖 **Подробная документация:** `/docs/AUTO_GENERATED_FAQ.md`
🚀 **Быстрый старт:** `/docs/QUICK_START_FAQ.md`
