# Автогенерация FAQ для товаров

## Обзор

Двухуровневая система генерации вопросов:

- **SQL-генерация**: Базовые вопросы для всех товаров (быстро, бесплатно)
- **AI-генерация**: Уникальные премиум-вопросы для дорогих товаров > 50,000₸ (Claude API)

## Типы генерируемых вопросов

### Базовые вопросы (SQL для всех товаров)

#### 1. Вопрос про возраст

**Условие:** Есть `min_age_years` или `max_age_years`

```
Q: С какого возраста можно играть с этой игрушкой?
A: Производитель рекомендует для детей от 3 до 6 лет.
```

#### 2. Вопрос про доставку

**Условие:** Всегда

```
Q: Как быстро доставите в Алматы?
A: Доставка по Алматы занимает 1-2 рабочих дня. Доставка бесплатна при заказе от 10 000 ₸.
```

#### 3. Вопрос про возврат

**Условие:** Всегда

```
Q: Можно ли вернуть товар?
A: Да, вы можете вернуть товар в течение 14 дней с момента получения, если он не был в использовании и сохранена упаковка.
```

### Премиум вопросы (AI для товаров > 50,000₸)

**Генерируются через Claude API** на основе:

- Название товара
- Описание
- Бренд, материал, страна
- Категория
- Цена

**Примеры AI-вопросов:**

```
Q: Почему этот конструктор стоит 65 000 ₸? В чём отличие от обычных?
A: Это не просто конструктор, а развивающая STEM-система от LEGO Education.
   В комплекте 1200+ деталей, включая редкие элементы шестерён и датчиков.
   Бренд LEGO гарантирует точность соединений и долговечность на годы —
   детали из качественного ABS-пластика не выцветают и не деформируются.
   Обычные конструкторы через год рассыпаются, а этот прослужит всю жизнь.

Q: Действительно ли ребёнок научится чему-то полезному с этим набором?
A: Да! Этот набор развивает инженерное мышление, пространственное воображение
   и навыки решения задач. В комплекте идёт обучающее приложение с 20+ проектами
   разной сложности. Дети учатся основам механики, программирования и робототехники
   в игровой форме. Многие родители отмечают, что дети от 8 лет могут часами
   собирать и улучшать свои модели.

Q: Стоит ли брать, если у нас уже есть другие конструкторы?
A: Однозначно стоит! Детали LEGO Technic совместимы со стандартными кубиками,
   но открывают новый уровень сложности. Здесь используются шестерни, оси,
   балки и моторы — ребёнок перейдёт от простой сборки к созданию механизмов.
   Это качественный скачок в развитии навыков конструирования.
```

**Особенности AI-генерации:**

- Подчёркивает ценность и уникальность
- Отвечает на возражения ("почему так дорого?")
- Использует эмоциональные триггеры
- Опирается на конкретные детали товара

## Использование

### Для одного товара (страница редактирования)

1. Перейти в админку → Товары → Выбрать товар
2. Нажать кнопку **"Сгенерировать FAQ"**
3. **Для обычных товаров** (< 50,000₸):
   - Генерируются только базовые вопросы (SQL)
   - Занимает 1-2 секунды
4. **Для премиум товаров** (> 50,000₸):
   - Появится подтверждение с информацией об AI-генерации
   - Генерируются базовые + AI вопросы
   - Занимает 5-10 секунд (запрос к Claude API)
5. Вопросы появятся на странице товара

### Массовая генерация (для всех товаров)

⚠️ **ВАЖНО:** Генерируются **ТОЛЬКО базовые вопросы** (без AI)

1. Перейти в админку → Товары (список)
2. Нажать кнопку **"Сгенерировать FAQ для всех"**
3. Подтвердить действие
4. Дождаться завершения (1-5 минут для 1000 товаров)
5. Уведомление покажет:
   - Сколько вопросов создано
   - Сколько премиум товаров требуют AI-генерацию
6. Для премиум товаров запустите AI-генерацию **индивидуально**

💡 **Почему AI не работает массово?**

- Экономия токенов Claude API (каждый запрос ~$0.01-0.03)
- Контроль качества (лучше проверять AI-ответы вручную)
- Скорость (AI генерация 1000 товаров займёт часы)

## Технические детали

### База данных

- **Таблица:** `product_questions`
- **Поле:** `is_auto_generated` (boolean) — маркирует автогенерированные вопросы
- **RPC функции:**
  - `generate_product_questions(product_id, skip_ai)` — генерация для одного товара
    - Возвращает JSON: `{needs_ai: boolean, product_id, name, price, ...}`
    - Если `price > 50000` и `skip_ai = false` → возвращает данные для AI
  - `generate_questions_for_all_products()` — массовая генерация (skip_ai = true)

### Логика работы

**Шаг 1: SQL-генерация (всегда)**

1. Удаляются старые автогенерированные вопросы
2. Создаются базовые вопросы (возраст, доставка, возврат)
3. Пользовательские вопросы (`is_auto_generated = false`) сохраняются

**Шаг 2: AI-генерация (если price > 50,000₸ и не skip_ai)**

1. RPC функция возвращает `needs_ai: true` + данные товара
2. Frontend вызывает Edge Function `generate-premium-questions`
3. Edge Function отправляет данные товара в Claude API
4. Claude генерирует 3-4 уникальных вопроса-ответа
5. Вопросы сохраняются в БД с `is_auto_generated = true`

### Edge Function: generate-premium-questions

**Путь:** `supabase/functions/generate-premium-questions/index.ts`

**Модель:** `claude-3-5-haiku-20241022` (быстрая, дешёвая)

**Стоимость:** ~$0.01-0.03 за товар (2000 токенов)

**Промпт:**

- Фокус на убеждении покупателя
- Подчёркивание ценности премиум-товара
- Ответы на возражения ("почему так дорого?")
- Конкретные детали (бренд, материал, особенности)

### Визуальное отличие

Автогенерированные вопросы имеют:

- Иконку ✨ (sparkles) вместо 🙋 (help-circle)
- Синий цвет фона иконки
- Метку "Часто задаваемый вопрос" вместо имени пользователя
- **Невозможно удалить** через UI (только через базу данных)

## SEO преимущества

1. **Rich Snippets**: Google может показывать FAQ в результатах поиска
2. **Keyword Coverage**: Вопросы содержат популярные запросы ("возраст", "оригинал", "доставка")
3. **User Experience**: Покупатели получают ответы до контакта с поддержкой
4. **Content Volume**: Больше текста на странице → лучше индексация

## Настройка

### 1. API ключ Claude

Для AI-генерации нужен API ключ Anthropic:

```bash
# Получить ключ: https://console.anthropic.com/settings/keys

# Установить секрет в Supabase
supabase secrets set ANTHROPIC_API_KEY=sk-ant-...

# Проверить
supabase secrets list
```

### 2. Деплой Edge Function

```bash
# Деплой функции
supabase functions deploy generate-premium-questions

# Проверить логи
supabase functions logs generate-premium-questions
```

## Кастомизация

### Изменить порог премиум-товара

По умолчанию: `price > 50000`

Изменить в файле:

```
supabase/migrations/20260128070000_add_auto_generated_questions.sql
```

```sql
-- Найти и изменить
IF v_price > 50000 AND NOT p_skip_ai THEN
             ^^^^^ -- Новое значение
```

### Изменить базовые вопросы (SQL)

Отредактируйте файл миграции:

```sql
INSERT INTO public.product_questions (...)
VALUES (
  p_product_id,
  NULL,
  'Ваш новый вопрос?',  -- ← Здесь
  'Ваш новый ответ.',    -- ← Здесь
  true,
  NOW()
);
```

После изменения:

```bash
supabase db reset
```

### Добавить новый тип вопроса

1. Добавьте `DECLARE` переменную (например, `v_weight NUMERIC`)
2. Добавьте в `SELECT` запрос (например, `p.weight`)
3. Добавьте условие и `INSERT` блок

Пример (вопрос про вес):

```sql
IF v_weight > 5 THEN
  INSERT INTO public.product_questions (...)
  VALUES (
    p_product_id,
    NULL,
    'Какой вес у товара?',
    'Вес товара: ' || v_weight || ' кг.',
    true,
    NOW()
  );
END IF;
```

## Часто задаваемые вопросы

### Сколько стоит AI-генерация?

~$0.01-0.03 за товар (модель Claude 3.5 Haiku, ~2000 токенов).

Для 100 премиум товаров: ~$1-3.

### Можно ли использовать другую AI-модель?

Да, отредактируйте `generate-premium-questions/index.ts`:

```typescript
model: 'claude-3-5-haiku-20241022',  // ← Изменить здесь
```

Доступные модели:

- `claude-3-5-haiku-20241022` — быстро, дёшево ($0.01/1000 токенов)
- `claude-3-5-sonnet-20241022` — умнее, дороже ($0.015/1000 токенов)

### Можно ли редактировать AI-вопросы?

Да, через базу данных напрямую. В UI убрана кнопка "Удалить" для всех автовопросов.

### Что будет, если запустить генерацию повторно?

Все старые автогенерированные вопросы (SQL + AI) **удалятся**, создадутся новые.

### Влияет ли на пользовательские вопросы?

Нет, пользовательские вопросы (`is_auto_generated = false`) сохраняются.

### Сколько времени занимает генерация?

- **Один товар (базовый)**: 1-2 секунды
- **Один товар (премиум)**: 5-10 секунд (SQL + AI)
- **Массово (1000 товаров)**: 1-5 минут (только SQL)

### Можно ли запустить генерацию через SQL?

Да:

```sql
-- Для одного товара (с AI)
SELECT generate_product_questions('product-uuid-here', false);

-- Для одного товара (без AI)
SELECT generate_product_questions('product-uuid-here', true);

-- Для всех товаров (без AI)
SELECT * FROM generate_questions_for_all_products();
```

### Как проверить качество AI-вопросов?

1. Сгенерируйте для 2-3 тестовых товаров
2. Проверьте вопросы на странице товара
3. Если нужно улучшить — отредактируйте промпт в `generate-premium-questions/index.ts`
4. Передеплойте функцию: `supabase functions deploy generate-premium-questions`

## Roadmap

- [ ] Добавить вопросы про гарантию (если есть поле `warranty_months`)
- [ ] Добавить вопросы про сборку (для конструкторов, мебели)
- [ ] Генерация на основе категории (разные вопросы для разных категорий)
- [ ] AI-генерация вопросов через Claude API (более персонализированные)
- [ ] Админка: предпросмотр вопросов перед применением
