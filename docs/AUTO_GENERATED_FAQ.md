# Автогенерация FAQ для товаров

## Обзор

Система автоматической генерации "умных вопросов" для товаров на основе их характеристик (возраст, бренд, материал, страна производства, цена).

## Типы генерируемых вопросов

### 1. Вопрос про возраст
**Условие:** Есть `min_age_years` или `max_age_years`

```
Q: С какого возраста можно играть с этой игрушкой?
A: Производитель рекомендует для детей от 3 до 6 лет.
```

### 2. Вопрос про оригинальность
**Условие:** Есть `brand_id`

```
Q: Это оригинальный товар?
A: Да, это 100% оригинальная продукция бренда Полесье. Мы работаем только с официальными поставщиками.
```

### 3. Вопрос про материал
**Условие:** Есть `material_id`

```
Q: Из какого материала изготовлена игрушка?
A: Игрушка изготовлена из пластика. Материал безопасен для детей и соответствует стандартам качества.
```

### 4. Вопрос про страну производства
**Условие:** Есть `origin_country_id`

```
Q: Где произведена игрушка?
A: Страна производства: Китай.
```

### 5. Вопрос про доставку
**Условие:** Всегда

```
Q: Как быстро доставите в Алматы?
A: Доставка по Алматы занимает 1-2 рабочих дня. Доставка бесплатна при заказе от 10 000 ₸.
```

### 6. Вопрос про возврат
**Условие:** Всегда

```
Q: Можно ли вернуть товар?
A: Да, вы можете вернуть товар в течение 14 дней с момента получения, если он не был в использовании и сохранена упаковка.
```

### 7. Вопрос про наличие
**Условие:** `price > 20000` (для дорогих товаров)

```
Q: Товар точно есть в наличии?
A: Да, товар в наличии на нашем складе в Алматы. Информация о наличии обновляется в режиме реального времени.
```

## Использование

### Для одного товара (страница редактирования)

1. Перейти в админку → Товары → Выбрать товар
2. Нажать кнопку **"Сгенерировать FAQ"** в правом верхнем углу
3. Дождаться завершения (1-2 секунды)
4. Вопросы появятся в разделе "Вопросы и ответы" на странице товара

### Массовая генерация (для всех товаров)

⚠️ **ВНИМАНИЕ:** Эта операция может занять несколько минут!

1. Перейти в админку → Товары (список)
2. Нажать кнопку **"Сгенерировать FAQ для всех"**
3. Подтвердить действие в диалоговом окне
4. Дождаться завершения
5. Увидите уведомление: "✨ Сгенерировано X вопросов для Y товаров"

## Технические детали

### База данных

- **Таблица:** `product_questions`
- **Поле:** `is_auto_generated` (boolean) — маркирует автогенерированные вопросы
- **RPC функции:**
  - `generate_product_questions(product_id)` — генерация для одного товара
  - `generate_questions_for_all_products()` — массовая генерация

### Логика работы

1. При генерации **старые автогенерированные вопросы удаляются**
2. Пользовательские вопросы (где `is_auto_generated = false`) **сохраняются**
3. Новые вопросы создаются с `user_id = NULL` и `answered_at = NOW()`
4. Вопросы сразу отображаются как "отвеченные"

### Визуальное отличие

Автогенерированные вопросы имеют:
- Иконку ✨ (sparkles) вместо 🙋 (help-circle)
- Синий цвет фона иконки
- Метку "Часто задаваемый вопрос" вместо имени пользователя
- **Невозможно удалить** через UI (только через базу данных)

## SEO преимущества

1. **Rich Snippets**: Google может показывать FAQ в результатах поиска
2. **Keyword Coverage**: Вопросы содержат популярные запросы ("возраст", "оригинал", "доставка")
3. **User Experience**: Покупатели получают ответы до контакта с поддержкой
4. **Content Volume**: Больше текста на странице → лучше индексация

## Кастомизация

### Изменить шаблоны вопросов

Отредактируйте файл:
```
supabase/migrations/20260128070000_add_auto_generated_questions.sql
```

Пример:
```sql
INSERT INTO public.product_questions (...)
VALUES (
  p_product_id,
  NULL,
  'Ваш новый вопрос?',  -- ← Здесь
  'Ваш новый ответ.',    -- ← Здесь
  true,
  NOW()
);
```

После изменения примените миграцию:
```bash
supabase db reset
```

### Добавить новый тип вопроса

1. Добавьте `DECLARE` переменную (например, `v_weight NUMERIC`)
2. Добавьте в `SELECT` запрос (например, `p.weight`)
3. Добавьте условие и `INSERT` блок

Пример (вопрос про вес):
```sql
IF v_weight > 5 THEN
  INSERT INTO public.product_questions (...)
  VALUES (
    p_product_id,
    NULL,
    'Какой вес у товара?',
    'Вес товара: ' || v_weight || ' кг.',
    true,
    NOW()
  );
END IF;
```

## Часто задаваемые вопросы

### Можно ли редактировать автогенерированные вопросы?

Да, через базу данных напрямую. В UI намеренно убрана кнопка "Удалить" для них.

### Что будет, если запустить генерацию повторно?

Старые автогенерированные вопросы **удалятся**, новые создадутся с актуальными данными товара.

### Влияет ли на пользовательские вопросы?

Нет, пользовательские вопросы (`is_auto_generated = false`) не затрагиваются.

### Сколько времени занимает массовая генерация?

Примерно 0.5-1 секунда на товар. Для 1000 товаров: 8-15 минут.

### Можно ли запустить генерацию через SQL?

Да:
```sql
-- Для одного товара
SELECT generate_product_questions('product-uuid-here');

-- Для всех товаров
SELECT * FROM generate_questions_for_all_products();
```

## Roadmap

- [ ] Добавить вопросы про гарантию (если есть поле `warranty_months`)
- [ ] Добавить вопросы про сборку (для конструкторов, мебели)
- [ ] Генерация на основе категории (разные вопросы для разных категорий)
- [ ] AI-генерация вопросов через Claude API (более персонализированные)
- [ ] Админка: предпросмотр вопросов перед применением
